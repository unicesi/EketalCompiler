/*
 * generated by Xtext 2.10.0
 */
package co.edu.icesi.eketal.ui.wizard;

import org.eclipse.xtext.ui.wizard.XtextNewProjectWizard;

import org.eclipse.xtext.ui.wizard.IExtendedProjectInfo;
import org.eclipse.xtext.ui.wizard.IProjectCreator;
import com.google.inject.Inject;

public class EketalNewProjectWizard extends XtextNewProjectWizard {

	private EketalWizardNewProjectCreationPage mainPage;

	@Inject
	public EketalNewProjectWizard(IProjectCreator projectCreator) {
		super(projectCreator);
		setWindowTitle("New Eketal Project");
	}

	protected EketalWizardNewProjectCreationPage getMainPage() {
		return mainPage;
	}

	/**
	 * Use this method to add pages to the wizard.
	 * The one-time generated version of this class will add a default new project page to the wizard.
	 */
	@Override
	public void addPages() {
		mainPage = createMainPage("basicNewProjectPage");
		mainPage.setTitle("Eketal Project");
		mainPage.setDescription("Create a new Eketal project.");
		addPage(mainPage);
	}

	protected EketalWizardNewProjectCreationPage createMainPage(String pageName) {
		return new EketalWizardNewProjectCreationPage(pageName);
	}

	/**
	 * Use this method to read the project settings from the wizard pages and feed them into the project info class.
	 */
	@Override
	protected IExtendedProjectInfo getProjectInfo() {
		EketalProjectInfo projectInfo = new EketalProjectInfo();
		projectInfo.setProjectName(mainPage.getProjectName());
		if (!mainPage.useDefaults()) {
			projectInfo.setLocationPath(mainPage.getLocationPath());
		}
		return projectInfo;
	}

}
